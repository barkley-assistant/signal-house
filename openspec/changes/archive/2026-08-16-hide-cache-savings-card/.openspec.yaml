schema: spec-driven
created: 2026-08-16
goal: Temporarily hide the Cache Savings section on the dashboard. The card
  itself, its component, and its data pipeline should remain untouched — only
  its render in App.tsx should be skipped.
